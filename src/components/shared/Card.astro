---
const {
  class: className = "",
  hoverable = false,
  background = "white",
} = Astro.props;
---

<div
  class={`
  bg-${background} rounded-2xl p-6
  ${hoverable ? "transition-all hover:shadow-lg hover:-translate-y-1 hover:-translate-x-1" : ""}
  ${className}
`}
>
  <slot />
</div>
